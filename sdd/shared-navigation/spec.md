# Shared navigation

## Requirement

Use the portfolio-family header pattern instead of a persistent application sidebar.

## Acceptance criteria

- Theme, language, Portfolio, cart, and Menu share one utility group.
- Menu exposes all five localized views and preserves active-view behavior.
- The former left sidebar and its content offset are removed.
- The header remains usable on narrow mobile screens.
