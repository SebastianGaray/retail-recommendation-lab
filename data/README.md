# Data workspace

Generated inputs belong in `raw/`, normalized data in `intermediate/`, and neither is committed by default. The versioned demo contract lives in `artifacts/demo/catalog.json`. All project data is synthetic.
